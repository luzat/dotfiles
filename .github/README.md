# dotfiles and bootstrap

These dotfiles are managed with [yadm](https://yadm.io/). They are mostly focused on a Debian or Kali environment with
tmux, vim and zsh.

## Installation

```shell
sh -c "`curl -fsL https://github.com/luzat/dotfiles/raw/main/.config/yadm/install.sh`"
```

